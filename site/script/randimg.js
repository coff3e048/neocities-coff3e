// Generated by CoffeeScript 2.6.1
(function() {
  // @license magnet:?xt=urn:btih:5305d91886084f776adcf57509a648432709a7c7&dn=x11.txt X11
  var count, gifs, imgfooter, preloadImg, randImg, randimg;

  gifs = ['skelly', 'cdhand', 'monitorhell', 'monitorkeybhell', 'cassette', 'roach', 'spacelaptop', 'laptop', 'desktop_pizza', 'classicdesktop', 'imac', 'motherboard', 'vaporboard', 'win9x', 'abstractness', 'brokencd', 'skellymonitor', 'aolfloppy1', 'aolmacfloppy', 'aolfloppy25', 'pizzafloppy', 'cursortrail', '95cd', 'sd1gb', 'alien'];

  randimg = document.getElementById("randimg");

  imgfooter = document.getElementById("imgfooter");

  preloadImg = function() {
    var i, j, len, results;
    results = [];
    for (j = 0, len = gifs.length; j < len; j++) {
      i = gifs[j];
      results.push((new Image()).src = '/gif/' + gifs[i] + '.gif');
    }
    return results;
  };

  count = 0;

  randImg = function() {
    var r;
    r = gifs[Math.floor(Math.random() * gifs.length)];
    if (count <= 100) {
      return randimg.src = "/gif/" + r + ".gif";
    } else {
      return randimg.src = "/images/troll.png";
    }
  };

  randimg.onclick = function() {
    var txt;
    txt = "";
    // placeholders
    randImg();
    if (count >= 5) {
      txt = "";
    }
    if (count >= 25) {
      txt = "";
    }
    if (count >= 50) {
      txt = "";
    }
    if (count >= 75) {
      txt = "";
    }
    if (count >= 100) {
      txt = "lol";
    }
    // Count corresponds to the amount of clicks on the image.
    imgfooter.innerHTML = txt;
    return count++;
  };

  window.onload = function() {
    preloadImg();
    return randImg();
  };

  // @license-end

}).call(this);
